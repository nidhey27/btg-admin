import { NavbarTrimPipe } from './navbar-trim.pipe';

describe('NavbarTrimPipe', () => {
  it('create an instance', () => {
    const pipe = new NavbarTrimPipe();
    expect(pipe).toBeTruthy();
  });
});
